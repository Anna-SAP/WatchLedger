# 本次构建操作记录

日期：2026-09-06。所有交付代码位于本文件夹；中间测试位于工作目录 work/。

1. 只读检查当前目录、Node/Python 可用性、项目指引；目录中没有待修改的现有应用。
2. 轻量搜索记忆索引，没有采用与本实现相关的历史资料；查阅 Chrome 与 Mozilla 官方扩展文档。
3. 创建扩展、Python 服务、SQLite 模型、中文界面、构建与启动脚本。
4. 首轮测试发现 SQLite 连接没有关闭，导致 Windows 无法释放测试文件。已改成保证提交/回滚并关闭的上下文管理器。
5. 系统 Node 因沙箱 realpath 权限失败；使用 Codex 随附 Node 成功构建 Neo 和 Firefox 两个目录。
6. 重跑通过 7 项 JavaScript 核心测试和 6 项 Python 数据库/API 测试。
7. Playwright 自带 Chromium 未安装；改用电脑已有 Edge 的无头模式及独立 work/test-profile。没有访问用户现有浏览器配置。
8. 实际加载 Neo 构建产物，播放本地生成音频、暂停、跳转和两倍速；通过扩展弹窗触发配对同步并读回 SQLite。第一轮得到 6 个片段、约 17.77 秒播放；无页面 JavaScript 错误。
9. 检查实际界面截图，发现 Edge 新版本也暴露 browser 命名空间，导致浏览器标签误标 Firefox。改为按构建 manifest 判断浏览器族，随后重跑验证。
10. 复测发现原隔离配置缓存旧版扩展，将测试改为每次新建独立配置后，最终版本通过：6 个片段、约 17.76 秒、1×/2×，浏览器标签正确、页面错误 0。最后一次核心测试仍为 JavaScript 7/7、Python 6/6。
11. 创建安装说明、可选登录自启动脚本和打包交付。未执行启动项安装，未安装到用户 Neo/Firefox，未上传代码或观看数据。

未验证：Neo 实际加载、Firefox 运行、各视频网站实站播放器、会员剧集与广告行为。Firefox 正式长期安装仍需签名流程。

安全范围：测试服务仅监听回环地址，测试结束关闭；测试库和浏览器配置留在 work/，不混入交付 ZIP。配对码不写入操作记录或截图。

## 2026-09-11：首页启动服务与连接图标（0.1.1）

- 在共用扩展首页右上角增加电源按钮，连接中禁用重复点击，并实时显示成功、认证失败和桥接安装帮助。原手工配对与待同步队列保持兼容。
- 新增 Windows Native Messaging 桥接：只接受固定 start 请求，隐藏启动同一个 server.py，检查真实服务状态后返回本机配对码。已运行时复用服务；跨浏览器启动用文件锁串行化。仅扩展首页能发起本地启动请求。
- 新增一次性 Install-Launcher.cmd；Start.cmd 的 PowerShell 入口也会注册桥接。只写当前用户 WatchLedger 桥接文件和 Chrome/Edge/Firefox 对应注册表项，不设置登录自启动。已在当前电脑执行安装器。
- 保持 Neo 按原目录生成的扩展 ID，未新增 manifest key；在隔离 Edge 中核对，安装器推导 ID 与实际加载 ID 一致。Firefox ID 不变。
- 工具栏使用绿/灰“视”图标，只有鉴权同步成功才变绿，断线或认证失败变灰并带警告。打开弹窗、后台重启和 30 秒同步都会刷新状态。
- JavaScript 核心及后台测试 17/17、Python 服务与桥接测试 11/11 通过；git diff --check 通过。
- 原真实 Edge 媒体采集测试通过：6 个片段、约 17.69 秒，包含暂停排除、跳转排除与 2 倍速，页面错误 0。
- 新真实 Edge 启动测试通过：从未配对状态点击电源按钮，真实调用注册桥接、启动服务、自动配对、绿色图标；错误配对码恢复灰色；重新连接成功；未保存的配对输入不会被存储刷新覆盖；关闭浏览器后服务仍可响应。测试使用独立浏览器配置，没有写入或删除观看记录。
- 已检查连接后截图：work/launcher-smoke-1789131500198/connected.png。构建产物已同步到 neo/ 和 firefox/。
- 未在用户正在使用的 Neo / Firefox 配置中重新加载或实测。用户需要在原扩展管理页重新加载，不能先卸载，以免清除尚未同步的片段。Neo 是否采用 Chrome Native Messaging 注册表回退仍需实际浏览器验证。
